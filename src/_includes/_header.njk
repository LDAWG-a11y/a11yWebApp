<a class="skip-link" href="#h1">Skip navigation</a>
<header class="header">
  <nav class="nav">
    <button class="nav__prefs-btn" id="prefsBtn">
      {% include '../svg/cog.svg' %}
      <span class="visually-hidden">Settings</span>
    </button>
    <button class="nav__menu-btn" id="navBtn" aria-controls="navPanel" aria-expanded="false" data-untouched>
      <span class="nav__menu-bar"></span>
      <span class="visually-hidden">Menu</span>
    </button>
    <div class="nav__wrapper" id="navPanel">
      <ul class="nav__list">
        {%- for item in navLinks %}
          {%- if item.mainNav == true %}
            <li class="nav__item {%- if item.collabBtn %} nav__item--parent{%- endif %}">
              <a class="nav__link"
                href="{{ item.url }}"
                {%- if item.url === page.url %}
                  aria-current="page"
                {%- endif %}>{{ item.text }}
              </a>
            </li>
          {%- endif %}
        {%- endfor %}
        <li class="nav__sub-items--parent">
          <button class="nav__btn--collab" aria-expanded="false" aria-controls="collabNavPanel" id="collabBtn">Collaborations</button>
          <div class="nav__list-sub-panel" id="collabNavPanel">
            <ul class="nav__list--level-2">
              {%- for collab in collaborations %}
                <li>
                  <h2 class="nav__list-collaborator">{{ collab.name }}</h2>
                  <ul class="nav__list--level-3">
                    {% for link in collab.pages %}
                      <li>
                        <a class="nav__link-sub"
                          href="{{ link.url }}"
                          {%- if link.url === page.url %}
                            aria-current="page"
                          {%- endif %}>
                          {{ link.text }}
                        </a>
                      </li>
                    {%- endfor %}
                  </ul>
                </li>
              {%- endfor %}
            </ul>
          </div>
        </li>
      </ul>
    </div>
  </nav>

  <picture class="header__logo">
    <source media="(prefers-color-scheme: light)" srcset="/img/MTA-logo-light.webp" height="136" width="600" data-main-logo>
    <source media="(prefers-color-scheme: light)" srcset="/img/MTA-logo-light.png" height="136" width="600" data-main-logo>
    <source media="(prefers-color-scheme: dark)" srcset="/img/MTA-logo-dark.webp" height="136" width="600" data-main-logo>
    <source media="(prefers-color-scheme: dark)" srcset="/img/MTA-logo-dark.png" height="136" width="600" data-main-logo>
    <img src="/img/MTA-logo-light.png" height="136" width="600" alt="Make Things Accessible, logo">
  </picture>

  {%- if not aaa %}
    <div class="header__search-block">
      <label id="sFilterLbl" for="sFilter" class="search__label">Search for Guides or Tags, by keyword</label>
      <div class="search__wrapper" data-expanded="false">
        <input class="search__input" id="sFilter" type="text" autocomplete="off">
        {% include '../svg/magnifier.svg' %}
      </div>
    </div>
  {%- endif %}

  {%- if aaa %}
    {%- if page.url !== "/aaa/" %}
      <picture class="header__aaa-logo header__aaa-logo--light">
        <img
        sizes="(max-width: 220px) 100vw, 220px"
        srcset="
        /aaaAssets/aaa-light_vpjlfm_c_scale,w_360.png 360w"
        src="/aaaAssets/aaa-light_vpjlfm_c_scale,w_360.png"
        alt="Access and Assistance for All logo">
      </picture>
      <picture class="header__aaa-logo header__aaa-logo--dark">
        <img
        sizes="(max-width: 220px) 100vw, 220px"
        srcset="
        /aaaAssets/aaa-dark_lmf9it_c_scale,w_360.png 360w"
        src="/aaaAssets/img/aaa-dark_lmf9it_c_scale,w_360.png"
        alt="Access and Assistance for All logo">
      </picture>
    {%- endif  %}
  {%- endif %}
</header>